import React from 'react'
import { Container, Carousel, Row, Col } from 'react-bootstrap'

export default function ArticleContainer(props) {
 const carouselCounter = 0
    
    const {artArr} = props
    console.log(artArr)
    return (
        <Container>
           
        </Container>
    )
}
